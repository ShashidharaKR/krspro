<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.user.advocate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="services.GPSTracker"
            android:enabled="true"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".activities.InitialScrn"/>
        <activity android:name=".activities.Login"/>
        <activity android:name=".activities.Admin"/>
        <activity android:name=".activities.AdminHome"/>
        <activity android:name=".activities.Advocate"/>
        <activity android:name=".activities.Advocate_network"/>
        <activity android:name=".activities.User"/>
        <activity android:name=".activities.Advocate_Profile"/>
        <activity android:name=".activities.Advocate_register"/>
        <activity android:name=".activities.Registration" />
        <activity android:name=".activities.Usr_Log"/>
        <activity android:name=".activities.User_Registration"/>
        <activity android:name=".activities.Otp_Verify"/>
        <activity android:name=".activities.Rating_Bar"/>
    </application>

</manifest>